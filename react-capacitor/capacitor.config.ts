import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vinckobb.mobile.framework.benchmark',
  appName: 'react-capacitor',
  webDir: 'out'
};

export default config;
